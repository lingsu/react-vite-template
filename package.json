{
  "name": "root",
  "private": true,
  "scripts": {
		"dev": "run-p dev:webapp dev:server",
		"dev:blade": "pnpm --filter ./packages/blade dev",
		"build": "run-p build:server build:webapp",
		"build:blade": "pnpm --filter ./packages/blade build",
		"build:deps": "pnpm --filter ./packages/utils build"
	},
  "devDependencies": {
    "lerna": "^8.1.2",
    "typescript": "^5.2.2",
    "typescript-node": "^0.1.3"
  }
}
